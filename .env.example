# Microsoft Azure AD App Registration
# Create at: https://portal.azure.com > Azure Active Directory > App registrations
AZURE_AD_CLIENT_ID=your-client-id-here
AZURE_AD_CLIENT_SECRET=your-client-secret-here
AZURE_AD_TENANT_ID=common

# NextAuth
# Generate with: openssl rand -base64 32
AUTH_SECRET=your-nextauth-secret-here
NEXTAUTH_URL=http://localhost:3000

# LiveKit Cloud — voice rooms
# Create a project at https://cloud.livekit.io, then copy from Project Settings
LIVEKIT_API_KEY=
LIVEKIT_API_SECRET=
NEXT_PUBLIC_LIVEKIT_URL=

# Optional — enables AI catch-up digests, action items, and chat summaries.
# Without it, the AI panels show a "not enabled" card.
OPENAI_API_KEY=

# Optional — point AI at an OpenAI-compatible endpoint instead of api.openai.com.
# e.g. an Azure AI Foundry deployment: https://<resource>.cognitiveservices.azure.com/openai/v1/
# (the model name then = your deployment name). Leave blank for OpenAI direct.
OPENAI_BASE_URL=

# Optional — per-user daily AI request limit (shared across all AI features).
# Defaults to 50/day. Enforcement needs Upstash Redis (see below); without
# Redis the quota fails open (AI works but is NOT capped per user).
AI_DAILY_REQUEST_LIMIT=50

# Optional — higher GitHub API rate limit for PR/issue cards (60/hr without, 5000/hr with)
GITHUB_TOKEN=

# Real-time channel messages via Graph webhooks need these Microsoft Graph permissions:
# - ChannelMessage.Read.All (delegated)
# Subscriptions require the app to be publicly reachable.
# In local dev, you need to use an ngrok tunnel:
#   ngrok http 3000
# Then override the webhook URL via:
# GRAPH_WEBHOOK_BASE_URL=https://your-tunnel.ngrok-free.app
GRAPH_WEBHOOK_BASE_URL=

# --- Optional: Upstash Redis (Vercel Marketplace) -----------------------------
# When set, realtime push (channels + DMs) works reliably across Vercel
# function instances, AND the per-user AI request quota is enforced. When
# unset, the app falls back to an in-memory transport plus the 30s poll
# (best-effort across instances) and the AI quota fails open (not enforced).
# Either naming works: the Upstash Vercel integration provisions the KV_* names;
# a manual/self-hosted setup can use the UPSTASH_* names.
# KV_REST_API_URL=            # or UPSTASH_REDIS_REST_URL=
# KV_REST_API_TOKEN=          # or UPSTASH_REDIS_REST_TOKEN=
