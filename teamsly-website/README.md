# Teamsly Website

Standalone static marketing site for Teamsly.

Open `index.html` directly in a browser. No build step or dev server is required.

Files:

- `index.html` - page structure and copy.
- `styles.css` - Slack-dark visual system and responsive layout.
- `script.js` - small interactions for status tabs and header state.
